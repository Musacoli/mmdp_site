sudo: required
language: node_js
node_js:
  - "9"
services:
  - docker

script:
  - echo "skipping tests"

jobs:
  include:
    - stage: build api
      script:
        - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
        - docker build -t mmdp_api -f ./Dockerfile-api .
        - docker images
        - docker tag mmdp_api $DOCKER_ID/mmdp_api
        - docker push $DOCKER_ID/mmdp_api
    - stage: build client
      script:
          - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
<<<<<<< HEAD
          - docker build -t mmdp_client -f ./Dockerfile-client .
=======
          - docker build --build-arg SERVER_APP_API_URL=$SERVER_APP_URL --build-arg NODE_ENV=$NODE_ENV --build-arg DEV_SERVER_API_URL=$DEV_SERVER_APP_URL  -t mmdp_client -f ./Dockerfile-client .
>>>>>>> feat(fix-remote-deployment): Fix deployment issues
          - docker images
          - docker tag mmdp_client $DOCKER_ID/mmdp_client
          - docker push $DOCKER_ID/mmdp_client
    - stage: build database
      script:
        - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
        - docker build -t mmdp_db -f ./Dockerfile-db .
        - docker images
        - docker tag mmdp_db $DOCKER_ID/mmdp_db
        - docker push $DOCKER_ID/mmdp_db
    - stage: build server
      script:
        - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
        - docker build -t mmdp_server -f ./Dockerfile-nginx .
        - docker images
        - docker tag mmdp_server $DOCKER_ID/mmdp_server
        - docker push $DOCKER_ID/mmdp_server

deploy:
  provider: elasticbeanstalk
  region: us-west-2
  app:  mmdp-cms
<<<<<<< HEAD
  env:  mmdp-cms-app
=======
  env:  MmdpCms-env-1
>>>>>>> feat(fix-remote-deployment): Fix deployment issues
  bucket_name: elasticbeanstalk-us-west-2-817932213731
  bucket_path: mmdp_cms
  on:
    branch: ft-fix-remote-deployment
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key:
    secure: $AWS_SECRET_KEY
