FROM node:alpine

WORKDIR $HOME/mmdp/

RUN apk --no-cache --virtual build-dependencies add \
    python \
    make \
    g++ \
    && yarn install \
    && apk del build-dependencies

COPY ./package.json ./

COPY ./ ./


EXPOSE 1234
CMD ["yarn", "run", "client:dev"]
