FROM node:alpine as builder

WORKDIR $HOME/mmdp/

ENV NODE_ENV="production"


COPY ./package.json ./

COPY ./server ./

RUN yarn install

CMD ["yarn", "run", "build:client"]

FROM nginx

COPY ./default.conf /etc/nginx/conf.d/default.conf

COPY — from=builder /mmdp/dist-client /usr/share/nginx/html

EXPOSE 80

