import React from 'react';
import Template from '../Templates';

const DashboardView = ({ ...props }) => (
  <Template {...props} title="Dashboard">
    <div>Dashboard content</div>
  </Template>
);

export default DashboardView;
